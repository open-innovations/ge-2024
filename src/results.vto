---
title: Results
---
<p>
  Download <a href="/results.json">this results file</a> (JSON format)
</p>

{{ if search.pages("constituency winner!=null").length > 0 }}
<table style="text-align: center;">
  <thead>
    <tr>
      <th>Constituency</th>
      <th>Elected</th>
    </tr>
  </thead>
  <tbody>
    {{ for c of search.pages("constituency winner!=null", "pcon24nm") }}
      <tr>
        <th scope="row">
          <a href="{{ c.url }}">
            {{ c.title }}
          </a>
        </th> 
        {{ if c.winner }}
          {{ comp.party({
            party: c.winner.party_key,
            name: c.winner.party_name + (c.results.confirmed ? "" : " (provisional)"),
            tag: 'td'
          })
        }}  
        {{ else }}
          <td>Result not yet available</td>
        {{ /if }}
      </tr>
    {{ /for }}
  </tbody>
</table>
{{ else }}
<p>
  No results have been recorded yet.
</p>
{{ /if }}